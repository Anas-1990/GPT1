version: '3.9'

services:
  backend:
    build: .
    container_name: ai_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app/backend
    restart: unless-stopped
